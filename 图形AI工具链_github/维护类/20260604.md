# 猜测 : 会话标识判断并更新导致图谱死锁,插件运行中状态无法更新
# 验证 : 事实上并不是死锁,而是由于那个持续的循环删除线程访问字典同时又有另一个方法也就是会话标识判断并更新方法访问了字典但是没有加锁所以触发异常被强制弹出,也没有走到返回, Python的Gil只能保证字节码操作的原子级但是字典的更改涉及到哈希表桶扩容总之并不是单一的字节码操作
以及这些线程不安全的操作会导致底层的索引混乱造成阻塞后续的执行完全崩溃这个应该是没return的根本原因
详细搜索: 问题1   问题2